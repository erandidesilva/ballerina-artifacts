# Http Connector Test BackEnd

### Notes to run:
- Go to webapp
- Run as mvn clean install
- After the build is success, go to the target folder, copy the .war file and deploy it in your favorite container.